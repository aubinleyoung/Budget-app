<section class="container">
<p style="color: green"><%= notice %></p>
<a href="<%= url_for('/users/index') %>" class="btn light-blue darken-4 text-ligth">Back</a>
<h2 class="center-align">Categories</h2>
<div  id="categories">

  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      
    </p>
  <% end %>
  <button  type="submit" name="action">
<%= link_to "Add category", new_category_path, class: "btn light-blue darken-4 text-ligth" %>
</button>
</div>

</section>
